{{ form_start(form, {'attr': {'class': ''}}) }}
{#    {{ form_widget(form.name, {'attr' : {'class':'form-control', 'placeholder':'mon place '}}) }}#}
<div class="row">
    <div class="col-md-4 offset-4">
        <label for="name">Nom</label>
        {{ form_widget(form.name, {'attr' : {'class':'form-control ', 'name': 'name'}}) }}
    </div>
</div>

<button class="btn btn-success col-md-4 offset-4 mt-2 ">{{ button_label|default('Sauvegarder') }}</button>
{{ form_end(form) }}
